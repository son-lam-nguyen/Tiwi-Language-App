<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.TiwiLanguageApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- White status & nav bars with dark icons -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>

        <!-- Dark icons -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.Rounded12" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <style name="Help.Bullet" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:drawableStart">@drawable/ic_bullet_dot</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textColor">#5A5A5A</item>
    </style>

    <style name="BottomNavText" parent="TextAppearance.Material3.LabelMedium">
        <item name="android:textSize">11sp</item>
    </style>

</resources>
