<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiwiLanguageApp">

        <!-- Single launcher: RoleSelectActivity -->
        <activity
            android:name=".RoleSelectActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TeacherLoginActivity" android:exported="false" />

        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".SentenceDetailActivity" android:exported="false" />
        <activity android:name=".FavoriteSentencesActivity"
            android:exported="false"
            android:label="Favorites"/>

        <activity android:name=".PhraseBookActivity" android:exported="false" />
        <activity android:name=".HelpActivity" android:exported="false" />

        <!-- FIX: add leading dot -->
        <activity
            android:name=".VideosActivity"
            android:exported="false"
            android:label="Cultural Videos" />

        <activity
            android:name=".PlayerWebActivity"
            android:exported="false"
            android:label="Video Player"
            android:configChanges="keyboardHidden|orientation|screenSize"/>

        <!-- Remove launcher filter from HomeActivity (keep or delete as a normal activity) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:label="About"/>

        <activity android:name=".QuizActivity" android:exported="false" />

        <activity
            android:name=".DonationActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity android:name=".AddSentenceActivity" />

    </application>
</manifest>